// import { pool } from "../../src/db.js";

// export const postNewEstado= async(req, res)=>{
//     const {estado, descripcion}=req.body;
//     const [rows] = await pool.query('insert into est_proc(estado, descripcion) value (?, ?)',[estado, descripcion])
//      res.send({rows})
// }